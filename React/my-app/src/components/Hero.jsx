import React from "react";

const Hero = (props) => {
  console.log(props);
  return (
    <div className="bg-amber-700">
      Hero
      <h3>Change About Layout color to red</h3>
    </div>
  );
};

export default Hero;
