Data always flows from the parent element which is the root to child and to sub-child all via props(properties). It can't go up from child to parent and can only go down.
Going from child to parent is possible but it requires a unique way.
