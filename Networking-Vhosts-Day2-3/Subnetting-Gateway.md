Subnetting is the division of IPv4 addresses into smaller address ranges.
The subnet is a logical segment of the network. In subnets, the first address is the network address used for delivery of data packet and the broadcast address is the last address for the subnet.

When a device from a certain subnet tries to communicate with a device from another subnet, the data pack must be routed to another subnet through the DEFAULT GATEWAY.
